#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve(){
}

int main()
{
    cin.tie(0)->sync_with_stdio(false);
    int T = 1;
    cin >> T;
    while (T--) {
        solve();
        cout << '\n';
    }
    return 0;
}
